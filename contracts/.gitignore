# Hardhat files
cache
artifacts
node_modules
.env

# Coverage reports
coverage
coverage.json

# Build info
build-info

# Deployment outputs — testnet/dev manifests excluded; mainnet manifests
# are permanent on-chain records and committed as audit artifacts.
deployments/*.json
!deployments/*-base-*.json

# Package manager
# package-lock.json is committed for reproducible CI builds (hardhat
# tests require it via `npm ci`). Re-add here if ever switching to a
# fresh lockfile workflow.
yarn.lock

# IDE
.vscode
.idea

# OS
.DS_Store
Thumbs.db

# Test outputs
test-results/
test-report.xml

# L1 static-tooling local outputs (CI uploads as artifacts;
# see audits/L1-static-tooling/README.md)
slither.sarif
aderyn-report.md
mythril-reports/
halmos-reports/
echidna-reports/

# Temporary files
*.tmp
*.temp
.tmp
ops-runbooks/
